-- Personal environment variables. Omarchy doesn't scaffold a slot for this
-- one, so it's required explicitly from hyprland.lua.

-- Used by hyprecise (~/.config/hyprecise), the pointer-precision helper bound
-- in hypr/bindings.lua.
hl.env("HYPRECISE_MODE", "auto")
hl.env("HYPRECISE_LOOP", "true")
